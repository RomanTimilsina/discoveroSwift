import UIKit

class OnlyForTest: UIViewController {
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        // Create and configure the custom slider view
        
        // Add the custom slider view to your view controller's view
//        view.addSubview(customSlider)
        // You can further customize the frame, position, and other properties as needed.
    }
}
